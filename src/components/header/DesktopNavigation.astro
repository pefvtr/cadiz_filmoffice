---
import { menuItems } from "@/components/header/data";

const currentPath = Astro.url.pathname;

interface Props {
  classNames?: string;
}
const { classNames } = Astro.props;
---

<div class:list={["main-menu", classNames]}>
  <nav class="main-nav">
    <ul class="">
      {
        menuItems.map((item) => (
          <a
            class:list={[
              "hover:text-primary px-2 transition-colors",
              {
                "text-primary pointer-events-none": currentPath === item.url,
                "text-black": currentPath !== item.url,
              },
            ]}
            href={item.url}
          >
            {item.label}
          </a>
        ))
      }
    </ul>
  </nav>
</div>
